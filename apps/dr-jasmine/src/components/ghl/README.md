# GHL section lift — deprecated (archive / reference)



**Status:** Deprecated as of Option A true-website rollout (T5/T11). **No route mounts these components at runtime.**



The public site uses native Astro pages (`PublicLayout`, `components/home/*`, `components/workshop/*`, etc.). GHL HTML fragments and Astro wrappers here remain for:



- Copy and structure reference when authoring native pages

- Regeneration via `scripts/lift-ghl-sections.mjs` after a new vault capture

- Historical comparison with `seo-wiki-vault/raw/research/dr-jasmine-ghl-capture/`



## Do not wire back without explicit product decision



Previously, `HomePage.astro` + `HomeLayout.astro` composed this folder for `/`. That path was retired in favor of Option A native home. Workshop conversion is **`/workshop`** (native), not a GHL fragment page.



## Related paths (still in repo)



| Path | Role |

|------|------|

| `fragments/*.html` | Lifted GHL section HTML (regenerated by scripts) |

| `fragments/SECTIONS_INVENTORY.md` | Fragment ↔ LDP inventory map (script-generated) |

| `remapHtml.ts`, `seoHtmlPass.ts` | Token remapping when fragments were rendered |

| `src/styles/ghl/*` | Sanitized capture CSS (only needed if remounting GHL preview) |

| `src/assets/ghl/*` | Localized images — **still used** by native About/Home/Workshop |

| `src/data/landing/images.ts` | Typed imports for portrait / Dan Henry / disclaimer bg |



## Regenerate lift outputs



See [`scripts/README.md`](../../../scripts/README.md). Vault capture under `seo-wiki-vault/raw/research/dr-jasmine-ghl-capture/` is **immutable** — scripts overwrite app outputs only.



## Conversion funnel (current)



- Marketing CTAs → `/dr-jasmine/workshop/` (`RegisterCta`, `ctaLabel`: “Join free workshop”)

- Workshop handoff → `drJasmineSiteConfig.registerUrl` (GHL external register)

- Dan Henry block → **`/workshop` only** (native `WorkshopDanHenry`, not `DanHenryCta.astro`)


