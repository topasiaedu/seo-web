---
/**
 * @fileoverview Hero headline + subcopy (`section-JznNLwNnfV`).
 */
import GhlFragment from "./GhlFragment.astro";
import fragment from "./fragments/hero.html?raw";

interface Props {
  registerUrl?: string;
}

const { registerUrl } = Astro.props;
---

<GhlFragment fragment={fragment} registerUrl={registerUrl} />
