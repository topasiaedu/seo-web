---
/**
 * @fileoverview Post-closing Dan Henry CTA strip (`section-bNQ2yZ6r2DO`).
 * Live DOM order places this after ClosingCta.
 */
import GhlFragment from "./GhlFragment.astro";
import fragment from "./fragments/dan-henry-cta.html?raw";

interface Props {
  registerUrl?: string;
}

const { registerUrl } = Astro.props;
---

<GhlFragment fragment={fragment} registerUrl={registerUrl} />
