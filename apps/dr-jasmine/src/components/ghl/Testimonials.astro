---
/**
 * @fileoverview Testimonials (`section-agbqSXhonD`).
 */
import GhlFragment from "./GhlFragment.astro";
import fragment from "./fragments/testimonials.html?raw";

interface Props {
  registerUrl?: string;
}

const { registerUrl } = Astro.props;
---

<GhlFragment fragment={fragment} registerUrl={registerUrl} />
