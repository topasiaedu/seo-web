---
/**
 * @fileoverview Top banner — free webinar (`section-GLe69CVwOE`).
 */
import GhlFragment from "./GhlFragment.astro";
import fragment from "./fragments/banner-webinar.html?raw";

interface Props {
  registerUrl?: string;
}

const { registerUrl } = Astro.props;
---

<GhlFragment fragment={fragment} registerUrl={registerUrl} />
