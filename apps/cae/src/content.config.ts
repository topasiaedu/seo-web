/**
 * @fileoverview Astro content-collections config for `@seo/cae`.
 *
 * Homepage copy lives in `src/data/home/*.ts` as plain TypeScript modules
 * (not Astro content collections). No collections are registered.
 */

/** No Astro content collections are defined for this app. */
export const collections = {};
